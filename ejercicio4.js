const nota1 = 4.5
const nota2 = 3.8
const nota3 = 2.9
const nota4 = 4.0

let suma = nota1 + nota2 + nota3 + nota4
let promedio = suma / 4
let operacion = (nota1 + nota2) * (nota3 + nota4) / 2
    // operacion = 8.3 * 6.9 / 2
    // operacion = 57.27 / 2
    // operacion = 28.635

    alert(`El resultado de las 4 notas sumadas es: ${suma} 
        El promedio de las notas es: ${promedio}
        Y la operacion resulta sería: ${operacion}`)